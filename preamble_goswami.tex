\PassOptionsToPackage{unicode=true,pdfwindowui,pdfpagemode=UseOutlines,colorlinks=true}{hyperref} % options for packages loaded elsewhere
\PassOptionsToPackage{hyphens}{url}
%%
\documentclass[11pt,a4paper,onecolumn,portrait]{article}
\usepackage{graphicx}
\usepackage{lmodern}
\usepackage{setspace}
\setstretch{1.0}
\usepackage{amssymb,amsmath}
\usepackage{ifxetex,ifluatex}
\usepackage{fixltx2e} % provides \textsubscript
\ifnum 0\ifxetex 1\fi\ifluatex 1\fi=0 % if pdftex
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provides euro and other symbols
\else % if luatex or xelatex
  \usepackage{unicode-math}
  \defaultfontfeatures{Ligatures=TeX,Scale=MatchLowercase}
\fi
% use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
% use microtype if available
\IfFileExists{microtype.sty}{%
\usepackage[]{microtype}
\UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\IfFileExists{parskip.sty}{%
\usepackage{parskip}
}{% else
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt plus 2pt minus 1pt}
}
\usepackage{hyperref}
\hypersetup{
            pdftitle={Inductive bias, climate networks, \& component analyses},
            pdfauthor={Bedartha Goswami},
            pdfborder={0 0 0},
            breaklinks=true}
\urlstyle{same}  % don't use monospace font for urls
\usepackage[top=0.75in,left=1.15in,right=1.15in,bottom=1.00in,heightrounded]{geometry}
\setlength{\emergencystretch}{3em}  % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\setcounter{secnumdepth}{5}
% Redefines (sub)paragraphs to behave more like sections
\ifx\paragraph\undefined\else
\let\oldparagraph\paragraph
\renewcommand{\paragraph}[1]{\oldparagraph{#1}\mbox{}}
\fi
\ifx\subparagraph\undefined\else
\let\oldsubparagraph\subparagraph
\renewcommand{\subparagraph}[1]{\oldsubparagraph{#1}\mbox{}}
\fi

% set default figure placement to htbp
\makeatletter
\def\fps@figure{htbp}
\makeatother

%%%% 
% Customise title
\usepackage{titling}
% the title
\pretitle{% add some rules
    \raggedright\sffamily\huge\bfseries
}%, make the fonts bigger, make the title (only) bold
\posttitle{%
  \linebreak\noindent
  \vrule height 1.0pt width \textwidth
  \vskip .25em % increase vertical spacing a bit
}
% the author name
\preauthor{%
    \raggedright\sffamily\Large \lineskip 0.25em%
}
\postauthor{%
    \vskip 0.05em
    \par
}
% the date
\predate{
    \raggedright\sffamily\normalsize
}
\postdate{
}
%%%
% Other changes
% change abstract formatting
\renewcommand{\abstractname}{Summary}
\renewenvironment{abstract}{%
    \hfill
    \begin{center}
        \begin{minipage}{0.75\textwidth}
        {\centering\large\sffamily\bfseries\abstractname\vskip 0.25em\par}
        \small
}
{%
        \vskip 1.50em\par\noindent
        %\rule{\textwidth}{1pt}
        \end{minipage}
    \end{center}
}
% format section and subsection headings
\usepackage{sectsty}
\allsectionsfont{\sffamily}
%%% Customise header and footer
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhead{}
\fancyfoot{}
\fancyhead[LO,L]{\footnotesize\itshape\theauthor} % \theauthor from titling
\fancyhead[RO,R]{\footnotesize\itshape\thetitle} % \theauthor from titling
%\fancyfoot[CO,CE]{So is this}
\fancyfoot[C,CO]{\footnotesize\thepage}
